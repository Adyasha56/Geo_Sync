# ========================================
# GeoSync Server Environment Variables
# ========================================

# Server Configuration
# Port the server will listen on
PORT=3001

# Client URL for CORS configuration
# Update this to match your frontend URL
# For local development: http://localhost:5173
# For production: https://your-frontend-domain.com
# Multiple origins: separate with commas
CLIENT_URL=http://localhost:5173

# ========================================
# Redis Configuration (Optional)
# ========================================
# If Redis is not configured, the server will use in-memory storage
# Note: In-memory storage is not suitable for production with multiple instances

# Option 1: Use REDIS_URL (Recommended for deployment)
# This is a connection string used by cloud providers (Upstash, Heroku, Railway, etc.)
# Format: redis://username:password@host:port or rediss:// for TLS
# If REDIS_URL is set, it takes priority over individual configs below
# REDIS_URL=redis://default:your_password@your-host.upstash.io:6379

# Option 2: Use individual Redis configs (For local development)
# Redis server hostname or IP address
REDIS_HOST=127.0.0.1

# Redis server port
REDIS_PORT=6379

# Redis password (if authentication is enabled)
# Leave commented out if no password is required
# REDIS_PASSWORD=your_redis_password_here

# ========================================
# Session Configuration
# ========================================

# Session expiry time in seconds
# Default: 3600 (1 hour)
# After this time, inactive sessions will be removed
SESSION_TTL=3600

# ========================================
# Notes
# ========================================
# - For local development, you can use the defaults above
# - For production:
#   1. Use a managed Redis instance (AWS ElastiCache, Redis Cloud, etc.)
#   2. Set strong passwords
#   3. Update CLIENT_URL to your production frontend domain
#   4. Consider increasing SESSION_TTL based on your use case
# - Never commit .env files with real credentials to version control
